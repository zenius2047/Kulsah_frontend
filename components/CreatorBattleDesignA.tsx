// Preserved first creator-battle concept: compact challenge-card carousel.
// It is intentionally not mounted in the feed; the active feed uses full-height
// participant video paging instead.
export { default } from './CreatorBattleCarousel';
